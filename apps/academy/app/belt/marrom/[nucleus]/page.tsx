import { notFound } from "next/navigation";
import nodes from "@taijifu/content/data/nodes";
import nuclei from "@taijifu/canon/data/nuclei";
import paths from "@taijifu/canon/data/paths";
import { BeltLessonActions, BeltNucleusProgress, BeltNucleusReflection } from "../../../components/belt-runtime-ui";
import { brownConfig } from "../../../components/brown-runtime";
export function generateStaticParams(){return(nuclei as any[]).filter(n=>n.beltId==="BELT-BROWN").map(n=>({nucleus:n.code.toLowerCase()}));}
export default async function NucleusPage({params}:{params:Promise<{nucleus:string}>}){const{nucleus}=await params;const code=nucleus.toUpperCase();const canonical=(nuclei as any[]).find(n=>n.code===code&&n.beltId==="BELT-BROWN");if(!canonical)notFound();const path=(paths as any[]).find(p=>p.id===canonical.pathId);const all=nodes as any[];const lesson=all.find(n=>n.canonicalEntityId===canonical.id&&n.type==="lesson");const quiz=all.find(n=>n.canonicalEntityId===canonical.id&&n.type==="quiz");const practice=all.find(n=>n.canonicalEntityId===canonical.id&&n.type==="guided-practice");if(!lesson||!quiz||!practice)notFound();const next=(nuclei as any[]).find(n=>n.order===canonical.order+1&&n.beltId==="BELT-BROWN");const previous=(nuclei as any[]).find(n=>n.order===canonical.order-1&&n.beltId==="BELT-BROWN");return <main><nav className="breadcrumb"><a href="/">Academy</a><span>/</span><a href="/belt/marrom">Faixa Marrom</a><span>/</span><strong>{canonical.code}</strong></nav><section className="lesson-hero"><div className="lesson-kicker"><span>{path.code}</span><span>{path.name}</span></div><div className="lesson-title-row"><div><span className="eyebrow">{canonical.code} · Núcleo oficial</span><h1>{canonical.name}</h1></div><BeltNucleusProgress config={brownConfig} nucleusId={canonical.id}/></div><p className="lead">{lesson.summary}</p><div className="lesson-meta"><span>≈ {lesson.durationMinutes+quiz.durationMinutes+practice.durationMinutes} min</span><span>Governança</span><span>Canon 1.0</span></div></section><section className="content-layout"><article className="lesson-content">{lesson.body.sections.map((section:any)=><section className={`content-section ${section.kind==="safety"?"safety-section":""}`} key={section.title}><span className="eyebrow">{section.kind}</span><h2>{section.title}</h2><p>{section.text}</p></section>)}<BeltLessonActions config={brownConfig} nucleusId={canonical.id} quizQuestions={quiz.questions} practiceText={practice.practice.instruction}/><BeltNucleusReflection config={brownConfig} nucleusId={canonical.id} pathId={path.id}/></article><aside className="lesson-aside"><div className="sticky-card"><span className="eyebrow">Contexto canônico</span><h3>{path.name}</h3><p>{path.function}</p><div className="aside-rule"><strong>Faixa</strong><span>Marrom · Governar</span></div><div className="aside-rule"><strong>Autoridade</strong><span>Role + Credential + Authorization</span></div><a className="text-link" href="/belt/marrom">← Voltar ao mapa</a></div></aside></section><nav className="lesson-nav">{previous?<a href={`/belt/marrom/${previous.code.toLowerCase()}`}><span>Anterior</span><strong>{previous.code} · {previous.name}</strong></a>:<span/>}{next?<a className="next" href={`/belt/marrom/${next.code.toLowerCase()}`}><span>Próximo</span><strong>{next.code} · {next.name} →</strong></a>:<a className="next" href="/belt/marrom"><span>Concluir</span><strong>Voltar ao mapa →</strong></a>}</nav></main>;}
