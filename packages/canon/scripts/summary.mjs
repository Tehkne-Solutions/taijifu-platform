import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const d=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../data'),load=n=>JSON.parse(fs.readFileSync(path.join(d,n),'utf8'));const belts=load('belts.json'),paths=load('paths.json'),names=load('nuclei.json');for(const b of belts){const ps=paths.filter(p=>p.beltId===b.id);console.log(`${String(b.order).padStart(2,'0')} ${b.name}: ${b.function} | paths=${ps.map(p=>p.code).join(',')||'síntese'}`)}console.log(`nuclei=${names.length} titled=${names.length} titlePending=0`);
