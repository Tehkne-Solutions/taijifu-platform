import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const cfg=JSON.parse(fs.readFileSync(path.join(root,'data/white-belt-content.json'),'utf8'));console.log(`content_nodes=${cfg.length*3}`);console.log(`content_edges=${cfg.length*3+(cfg.length*3-1)}`);console.log('lessons=12 quizzes=12 guided_practices=12');
