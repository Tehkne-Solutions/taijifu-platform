import fs from'node:fs';const read=p=>fs.readFileSync(p,'utf8');const store=read('apps/academy/app/components/evidence-store.ts'),belt=read('apps/academy/app/belt/branca/page.tsx'),lesson=read('apps/academy/app/belt/branca/[nucleus]/page.tsx');for(const marker of ['localStorage','promotionGranted: false','BELT-YELLOW'])if(!store.includes(marker))throw new Error(`missing UI state marker ${marker}`);for(const marker of ['/belt/branca/checkpoint','/belt/branca/travessia'])if(!belt.includes(marker))throw new Error(`missing white flow ${marker}`);if(!lesson.includes('LessonActions')||!lesson.includes('NucleusReflection'))throw new Error('nucleus learning flow incomplete');console.log('TAIJIFU_UI_VALIDATION=PASS');console.log('flow=belt>path>nucleus>lesson>practice>checkpoint>reflection>path-checkpoint>traversal');console.log('belt=immutable-client-side');
