import fs from"node:fs";
const required=["apps/academy/app/belt/preta/page.tsx","apps/academy/app/components/black-synthesis.tsx"];
for(const file of required)if(!fs.existsSync(file))throw new Error(`missing ${file}`);
const page=fs.readFileSync("apps/academy/app/belt/preta/page.tsx","utf8");const view=fs.readFileSync("apps/academy/app/components/black-synthesis.tsx","utf8");const paths=JSON.parse(fs.readFileSync("packages/canon/data/paths.json","utf8"));const nuclei=JSON.parse(fs.readFileSync("packages/canon/data/nuclei.json","utf8"));
if(paths.length!==32)throw new Error(`paths=${paths.length}, expected=32`);if(nuclei.length!==128)throw new Error(`nuclei=${nuclei.length}, expected=128`);if(/PATH-C33|NUC-N129|C33|N129/.test(page+view))throw new Error("Black Belt must not create C33/N129");if(!view.includes("VIEW-SYN-SYSTEM")||!view.includes("VIEW-SYN-CONTINUITY"))throw new Error("Black synthesis views missing");if(/recordEvaluationAndPromotion|currentBeltId\s*=/.test(view))throw new Error("Black synthesis cannot mutate belt");console.log("TAIJIFU_BLACK_BELT_VALIDATION=PASS");console.log("belt=BELT-BLACK");console.log("function=SYNTHESIZE");console.log("canon_paths=C01-C32");console.log("canon_nuclei=N001-N128");console.log("new_paths=0");console.log("new_nuclei=0");console.log("canon_mutation=BLOCKED");console.log("belt_mutation=BLOCKED");
